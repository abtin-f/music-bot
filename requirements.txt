# Telegram framework
aiogram>=3.15,<4.0
aiohttp-socks>=0.9      # optional SOCKS proxy support for the Telegram session

# Downloader (pulls in yt-dlp, ffmpeg bindings are NOT included — install FFmpeg separately)
spotdl>=4.2.10

# Database
aiosqlite>=0.20.0

# Audio metadata + thumbnails
mutagen>=1.47.0
Pillow>=10.4.0

# Config
python-dotenv>=1.0.1
